# Counterfeit Detector - AI Agent Instructions

**Only run against public data or with the written authorization of the data owner (marketplace/platform).**

- Store your brand images or reference hashes in the Key-Value Store.
- Use browser mode only when marketplace ToS and law allow.
- Always respect site robots.txt and do not overload targets.
- Human review is required before acting on any findings.

**Run:**
- `npm install`
- `apify run`